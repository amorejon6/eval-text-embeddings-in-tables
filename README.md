# Evaluating Text Embedding Models in Tabular Dataset Retrieval

This repository contains **two complementary experiments** for evaluating table embeddings in retrieval tasks:

1. **exp1_row_tests**: Row-level embedding robustness tests (column reordering, deletion, etc.)
2. **exp2_retrieval_eval**: Table-to-table retrieval evaluation with different aggregation strategies